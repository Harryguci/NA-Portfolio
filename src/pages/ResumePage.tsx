import RoutePlaceholder from "../components/RoutePlaceholder";

const ResumePage = () => {
  return (
    <RoutePlaceholder
      title="Resume"
      description="This section will showcase my profile, skills, timeline, and selected achievements in the same dreamscape style."
    />
  );
};

export default ResumePage;
