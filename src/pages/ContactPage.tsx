import RoutePlaceholder from "../components/RoutePlaceholder";

const ContactPage = () => {
  return (
    <RoutePlaceholder
      title="Contact"
      description="This section will include my contact channels, collaboration form, and social links in a retro-window layout."
    />
  );
};

export default ContactPage;
