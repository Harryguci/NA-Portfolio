import RoutePlaceholder from "../components/RoutePlaceholder";

const WorksPage = () => {
  return (
    <RoutePlaceholder
      title="Works"
      description="This section will present case studies, campaign visuals, and interactive project breakdowns with category-based tabs."
    />
  );
};

export default WorksPage;
